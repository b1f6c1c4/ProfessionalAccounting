Interest帮助文档

利息收入插件InterestRevenue
别名：ir
总共3个参数，
第一个参数为借款代码（模糊匹配）；
第二个参数为利率；
第三个参数可选，若没有表示只计算上次计息至今的部分，若为日期表示只计算上次计息到该日期的部分，all表示从头开始计算至今。

利息支出插件InterestExpense
别名：ie
总共3个参数，
第一个参数为借款代码（模糊匹配）；
第二个参数为利率；
第三个参数可选，若没有表示只计算上次计息至今的部分，若为日期表示只计算上次计息到该日期的部分，all表示从头开始计算至今。
