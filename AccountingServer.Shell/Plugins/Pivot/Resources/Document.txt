Pivot帮助文档

数据透视图插件Pivot

$pvt$ ( <细目检索式>     <行-分类汇总参数>         )+ <列-分类汇总参数>          [t] [f]
$pvt$ ( <记账凭证检索式> <行-记账凭证分类汇总参数> )+ <列-记账凭证分类汇总参数>  [t] [f]
其中t表示转置，f表示自动调整列宽

注意：
每个行参数都不能与列参数重叠
等价货币必须同时出现在所有的参数上
若任何一个行参数包括累加，则列参数不能累加
若列参数包括累加，则任何一个行参数都不能累加
