THUInfo帮助文档

从info.tsinghua.edu.cn上自动更新账户插件THUInfo
若无参数，则自动对账并尝试自动记账；
若第一个参数为"ep"且参数个数为1，则显示终端编号列表；
若第一个参数为""且参数个数为3，则以第二个参数为用户名，第三个参数为密码更新消费记录；
若第一个参数为"whatif"且参数个数为1，则只进行对账；
若第一个参数非空，则每个参数表示一天的消费情况。
